# configs/datasets/manc.yaml
# Male Adult Nerve Cord (MANC) dataset configuration.

name: MANC
version: "1.0"
description: "Male Adult Nerve Cord connectome."
is_fafb: false

files:
  neurons: "neurons.csv.gz"
  connections: "connections_princeton.csv.gz"

required_neuron_columns:
  - root_id
required_connection_columns:
  - pre_root_id
  - post_root_id
